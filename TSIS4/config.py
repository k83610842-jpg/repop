# здесь храню все числа и настройки чтоб не искать их по всему коду

TILE = 20       # один квадратик на поле = 20 пикселей
COLS = 30       # 30 колонок
ROWS = 30       # 30 строк
WIDTH  = COLS * TILE        # ширина окна
HEIGHT = ROWS * TILE + 40   # высота + место под HUD сверху
GRID_TOP = 40               # HUD занимает 40px сверху, поле начинается ниже

HIGHSCORE_FILE = "highscore.txt"  # рекорд сохраняю сюда
SETTINGS_FILE  = "settings.json"  # настройки сюда (task 3.5)

# дефолтные настройки  используются если settings.json ещё не создан
DEFAULT_SETTINGS = {
    "snake_color": [0, 220, 0],  # зелёный по умолчанию
    "grid_overlay": False,        # сетка выключена
}

# 4 вида еды  чем редче тем больше очков, но исчезает быстрее
FOOD_TYPES = [
    {"points": 1,  "color": (220, 60,  60),  "lifetime": None},  # красная, не исчезает
    {"points": 3,  "color": (255, 165,   0), "lifetime": 7},     # оранжевая, 7 секунд
    {"points": 5,  "color": (255, 215,   0), "lifetime": 5},     # жёлтая, 5 секунд
    {"points": 10, "color": (180,   0, 255), "lifetime": 3},     # фиолетовая, 3 секунды
]
FOOD_WEIGHTS = [0.50, 0.25, 0.15, 0.10]  # вероятность появления каждого типа

# 3 вида пауэрапов
POWERUP_TYPES = [
    {"kind": "speed",  "color": (0, 200, 255),  "label": "»"},   # ускорение
    {"kind": "slow",   "color": (200, 100, 255), "label": "«"},   # замедление
    {"kind": "shield", "color": (255, 230, 0),   "label": "O"},   # щит от одного удара
]
